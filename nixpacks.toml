[phases.setup]
nixPkgs = ['maven', 'jdk21']

[phases.build]
cmds = ['cd backend && mvn clean package -DskipTests']

[phases.start]
cmd = 'cd backend && java -Dserver.port=$PORT -jar target/bookaro-backend-1.0.3.jar'